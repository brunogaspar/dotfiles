
1. Ensure Mac is fully updated
2. Install XCode `xcode-select –-install`
3. Install 1Password and enable SSH Agent
4. Clone `git clone git@github.com:brunogaspar/dotfiles .dotfiles`
5. Install `sh install.sh`
